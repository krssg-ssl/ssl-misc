matrix:
  include:
    - os: linux
      dist: trusty

language : cpp

compiler : gcc

install:
  - cd ../ssl-misc
  - sudo chmod +x install.sh
  - user_=$USER
  - bash install.sh "/home/$user_"

script:
  - source /opt/ros/jade/setup.bash
  - cd /home/$user_/catkin_ws/
  - catkin_make krssg_ssl_msgs
  - catkin_make

notifications:
    email: false
